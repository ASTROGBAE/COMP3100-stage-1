public class ServerSchedule {
    // read from xml, get server type, server index and core number
    // all you care about is server type, index, core
    // use GETS command to get all servers of a specific type, iterate through and get servers of that type
    // dont need to worry about server ordering other than type, core? only worry about type?
    // can get XML from 'GETS system.xml'
}
